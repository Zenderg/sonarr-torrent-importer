services:
  importer:
    image: sonarr-torrent-importer:${IMPORTER_DEV_TAG:-dev}
    build:
      context: .
      args:
        VERSION: ${BUILD_VERSION:-dev}
        COMMIT_SHA: ${BUILD_COMMIT_SHA:-unknown}
        BUILD_TIME: ${BUILD_TIME:-unknown}
